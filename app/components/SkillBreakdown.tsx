"use client";

import Sidebar from "@/components/Sidebar";

export default function SkillBreakdown() {
  return (
    <div className="pl-20 p-6">
      <Sidebar />
      <h2 className="text-xl font-bold mb-4">
        Skill Breakdown per Tech (Coming Soon)
      </h2>
      <p>
        We will add visualization for subskills such as closures, promises, etc.
        under each main technology.
      </p>
    </div>
  );
}
